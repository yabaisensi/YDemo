package test;

import java.util.Scanner;

public class TestSnack {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		StringBuffer sb = new StringBuffer();
		if(!sc.hasNextLine()) System.exit(0);
		System.out.println("SCCCCCC "+sc.nextLine()); 
		if(!sc.hasNextLine()) System.exit(0);
		System.out.println("SCCCCCC "+sc.nextLine()); 
		if(!sc.hasNextLine()) System.exit(0);
		System.out.println("SCCCCCC "+sc.nextLine()); 
		if(!sc.hasNextLine()) System.exit(0);
		System.out.println("SCCCCCC "+sc.nextLine()); 
		if(!sc.hasNextLine()) System.exit(0);
		if(!sc.hasNextLine()) System.exit(0);
		if(!sc.hasNextLine()) System.exit(0);
		if(!sc.hasNextLine()) System.exit(0);
		if(!sc.hasNextLine()) System.exit(0);
//		nextStr.
//		while(sc.hasNextLine()) {
//			String nextStr =  sc.nextLine();
//			while (nextStr.contains("_")) {
//				nextStr = nextStr.replaceFirst("_[a-z]",
//					String.valueOf(Character.toUpperCase(nextStr.charAt(nextStr.indexOf("_") + 1))));
//			}
//			sb.append(nextStr+"\r\n");
//		}
		System.out.println(sb);
		System.out.println("END END");
		
		
//	    String cstr = sc.next();
//		String phrase = "always_use_camel_back_notation_in_java";
//		while (phrase.contains("_")) {
//			phrase = phrase.replaceFirst("_[a-z]",
//					String.valueOf(Character.toUpperCase(phrase.charAt(phrase.indexOf("_") + 1))));
//		}
//		System.out.println(phrase);
	}

}
